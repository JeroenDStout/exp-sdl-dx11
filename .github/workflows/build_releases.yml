name: Release
on:
  push:
    tags:
      - 'v*'

env:
  BUILD_TYPE: Release
  
permissions:
  contents: write
  
jobs:
  build:
    name: "Windows Latest MSVC"
    runs-on: "Windows-latest"
    steps:
    - uses: actions/checkout@v3
    
    - name: Checkout
      run: git fetch --unshallow --update-head-ok origin '+refs/heads/*:refs/heads/*'
          
    - name: Configure
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
      
    - name: Build
      run: cmake --build ${{github.workspace}}/build --config Release
      
    - name: Package
      run: python ${{github.workspace}}/util/create_release_zip.py
        mode:"Release"
        config:"Release"
        info:"${{github.workspace}}/build/publish_info.txt"
        out:"${{github.workspace}}/zip/experiment-release.zip"
        
    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        files:
          ${{github.workspace}}/zip/experiment-release.zip
